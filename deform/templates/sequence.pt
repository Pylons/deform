<div>
  <!-- sequence.html -->
  <input type="hidden" name="__start__" value="${field.name}:sequence"
         tal:attributes="prototype field.widget.prototype(field)"/>
  <div onclick="add_new_item(this);">${field.title} 
      <a href="#" class="deform_add"><small>(add)</small></a></div>
  <div tal:repeat="(item, f) subfields">
    <span tal:define="rndr field.renderer;
                      tmpl field.widget.item_template"
          tal:replace="structure rndr(tmpl, field=f, cstruct=item)"/>
  </div>
  <input type="hidden" name="__end__" value="${field.name}:sequence"/>

</div>
