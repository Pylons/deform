<li title="${field.description}">
  <p tal:condition="not field.widget.hidden"
         class="desc"
         title="${field.description}"
         >${field.title}</p>
  <span tal:replace="structure
            field.widget.serialize(field, cstruct, readonly=True)"/>
</li>
