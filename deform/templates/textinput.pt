<span tal:define="name name|field.name;
                  css_class css_class|field.widget.css_class;
                  css_class_full string:form-control ${css_class};
                  oid oid|field.oid;
                  mask mask|field.widget.mask;
                  mask_placeholder mask_placeholder|field.widget.mask_placeholder;
                  type_name type_name|field.widget.type_name;
                  style style|field.widget.style;
                  client_validators_class field.widget.client_validators;
                  "
      tal:omit-tag="">
    <input type="${type_name}" name="${name}" value="${cstruct}" 
          ng-model="fields.${oid}" id="${oid}"
          class="${css_class_full}" style="${style}" 
          tal:attributes="field.widget.client_validators"
          autocomplete="off">
    <script tal:condition="mask" type="text/javascript">
      deform.addCallback(
          '${oid}',
          function (oid) {
              if (angular !== undefined) {
                  $("#" + oid).mask("${mask}", 
                     {placeholder:"${mask_placeholder}"});
             });
      }
    </script>
</span>
